package webint.woodpecker.common.kafka

/*
@Component
class KafkaReceiver {
    @KafkaListener(topics = ["topic1"])
    fun receiveTopic1(consumerRecord: ConsumerRecord<*, *>) {
        println("Receiver on topic1: $consumerRecord")
    }

    @KafkaListener(topics = ["topic2"])
    fun receiveTopic2(consumerRecord: ConsumerRecord<*, *>) {
        println("Receiver on topic2: $consumerRecord")
    }
}
*/